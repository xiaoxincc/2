/*#include <iostream>
using namespace std;
int main() {
	int i;
	for (i = 0; i < 100; i++)
	{
		//如果是奇数则直接退出，是偶数则直接输出
		if (i % 2 != 0)
		{
			continue;//可以筛选条件，执行到此就不再向下执行，执行下一次循环
			//break会退出循环，而continue不会，continue并没有使整个循环终止
		}
		else {
			cout << i << endl;
		}
	}
	//continue语句
	//作用：在循环语句中，跳出本次循环中余下尚未执行的语句，继续执行下一次循环
	system("pause");
	return 0;
}*/